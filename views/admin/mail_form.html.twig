{% extends 'admin/layout-admin.html.twig' %}
{% set adminMenu = true %}

{% block title %}{{ title }}{% endblock %}

{% block content %}
<h2 class="text-center">{{ block('title') }}</h2>
{% for flashMessage in app.session.flashbag.get('success') %}
<div class="alert alert-success">
    {{ flashMessage }}
</div>
{% endfor %}

<div class="well">
{{ form_start(mailForm, { 'attr': {'class': 'form-horizontal'} }) }}
    <div class="form-group">
        {{ form_label(mailForm.title, null, { 'label_attr':  {
            'class': 'col-sm-4 control-label'
        }}) }}
        <div class="col-sm-6">
            {{ form_errors(mailForm.title) }}
            {{ form_widget(mailForm.title, { 'attr':  {
                'class': 'form-control'
            }}) }}
        </div>
    </div>
    <div class="form-group">
        {{ form_label(mailForm.content, null, { 'label_attr':  {
            'class': 'col-sm-4 control-label'
        }}) }}
        <div class="col-sm-6">
            {{ form_errors(mailForm.content) }}
            {{ form_widget(mailForm.content, { 'attr':  {
                'class': 'form-control',
                'rows': '8'
            }}) }}
        </div>
    </div>
    <div class="form-group">
        <div class="col-sm-offset-4 col-sm-3">
            <input formnovalidate="true" type="submit" class="btn btn-primary" value="Envoyer" />
        </div>
    </div>
{{ form_end(mailForm) }}
</div>

{% block javascript %}
    <script src='https://cloud.tinymce.com/stable/tinymce.min.js?apiKey=0p653klhvyitgrag0ua5i4yjzvl32q93da56pjz8jxzlac4z'></script>
    <script>
  tinymce.init({
    selector: 'textarea',
    plugins : 'advlist autolink link image lists charmap print preview',
  });
  </script>
{% endblock %}

{% endblock %}